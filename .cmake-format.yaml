# CMake formatter configuration for cmake-format
format:
  line_width: 120
  tab_size: 4
  max_subgroups_hwrap: 2
  max_pargs_hwrap: 6
  dangle_parens: true
  dangle_align: prefix
  command_case: lower
  keyword_case: upper
  always_wrap:
    - install

markup:
  canonicalize_hashrulers: false
